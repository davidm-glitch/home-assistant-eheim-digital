mqtt:
  host: # Add IP address of mqtt host
  port: 1883 # change if needed
  user: # Add mqtt username
  password: # Add mqtt password (in ' )
  qos: 1
  base_topic: eheim_digital
  home_assistant: True

general:
  host: # Add IP address of master device
  devices:
    - type: filter
      mac: # enter mac address
    - type: heater
      mac: # enter mac address
  log_level: info
